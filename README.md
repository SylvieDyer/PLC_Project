# Language Interpreter Project 

This project, built over the course of a semester interprets a text file of code. Developed in Racket, it will parse the file while keeping track of global and local variables, and executing proper control flows to return a value based on static scoping.
